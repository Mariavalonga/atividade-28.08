import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    title: {
        fontSize: 40,
        marginBottom: 5,
        fontWeight: 'bold',
        color: '#F9629F',
    }
    },
)
export default styles